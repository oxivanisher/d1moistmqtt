#ifndef __CONFIG_H
  #define __CONFIG_H

 /************************ Development settings ********************************/
  //#define DEBUG
  #define VERSION         "0.1"
  #define SERIAL_BAUD     115200

  /************************* WiFi Access Point *********************************/
  #define WIFI_SSID       ""
  #define WIFI_PASSWORD   ""
  #define CONNECT_TIMEOUT 5

  /***************************** MQTT Setup ***********************************/
  #define MQTT_SERVER     ""
  #define MQTT_SERVERPORT 1883
  #define MQTT_USERNAME   ""
  #define MQTT_PASSWORD   ""

  /*************************** Features Setup *********************************/
  // How often should be measured automatically
  #define MEASURE_EVERY 900
  // Sensor Pin where the capacitive soil moisture sensor is connected
  #define SENSOR_PIN A0
  // After running the calibration, insert the values here
  #define DRY 520
  #define WET 260

#endif
